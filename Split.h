#pragma once
#include <vector>
#include <string>
#include <sstream>
// header containing in common folder but .cpp file in server folder. move header to server folder
std::vector<std::string> split(const std::string& string, char delimiter);
